# tests

Contains cross-module integration tests, end-to-end tests, fixtures, and
migration parity assets.

Unit tests should generally live near the module they validate. Shared fixtures
and full workflow tests belong here.
